# 测试指南

本文档提供了邮件用户代理（Email User Agent）的测试指南，帮助验证所有新功能是否正常工作。

## 环境准备

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 准备测试邮箱

建议准备至少2个邮箱账号进行测试：
- 邮箱A：用于发送邮件
- 邮箱B：用于接收邮件（也可以是同一个邮箱）

支持的邮箱服务：
- QQ邮箱（需要开启SMTP/POP3服务并获取授权码）
- 163邮箱
- Sina邮箱
- 中科院邮箱
- 其他支持SMTP/POP3的邮箱

## 测试场景

### 测试1: 基本邮件发送和接收

**目的**: 验证基本的SMTP和POP3功能

**步骤**:
1. 启动程序: `python main.py`
2. 配置邮箱账号（设置 -> 账号管理）
3. 发送一封简单的邮件（不启用加密）
4. 使用接收功能查看邮件

**预期结果**:
- 邮件成功发送
- 可以正常接收和查看邮件
- 显示状态: "📧 普通邮件（未加密）"

### 测试2: 附件功能

**目的**: 验证附件发送和接收

**步骤**:
1. 发送邮件时点击"添加"按钮
2. 选择多个不同类型的附件：
   - 文本文件 (test.txt)
   - 图片文件 (image.jpg)
   - PDF文件 (document.pdf)
   - 音频文件 (audio.mp3)
3. 发送邮件
4. 接收邮件并查看附件列表

**预期结果**:
- 所有附件都成功发送
- 接收时可以看到附件列表
- 附件数量和文件名正确显示

### 测试3: 安全加密功能

**目的**: 验证基本的加密和解密功能

**步骤**:
1. 打开"设置" -> "高级设置"
2. 勾选"启用自定义Base64编码"
3. 输入共享密钥（例如: "TestSecret123456"）
4. 保存设置
5. 发送一封加密邮件
6. 在另一个客户端配置相同的共享密钥
7. 接收并查看邮件

**预期结果**:
- 使用相同密钥的客户端可以正常解密
- 显示状态: "✓ 已加密并验证 [配对UA]"
- 邮件内容正确显示

### 测试4: 附件加密（智能内容处理）

**目的**: 验证文本附件加密和多媒体附件保留

**步骤**:
1. 启用加密（参考测试3）
2. 发送包含以下附件的邮件：
   - 文本文件 (notes.txt)
   - 图片文件 (photo.png)
   - 文档文件 (report.docx)
   - 音频文件 (song.mp3)
3. 接收邮件

**预期结果**:
- 文本文件和文档被加密: 显示"✓ (已加密)"
- 图片和音频不加密: 显示"(未加密)"
- 邮件服务器可以识别有附件（通过Web邮箱查看）

### 测试5: 收件中心模式 - 配对UA通信

**目的**: 验证配对UA之间的通信

**准备工作**:
- 客户端A配置:
  - 共享密钥: "SharedSecret2024"
  - UA身份: "team-alpha-001"
- 客户端B配置:
  - 共享密钥: "SharedSecret2024"
  - UA身份: "team-alpha-001"

**步骤**:
1. 使用客户端A发送加密邮件到收件中心账户
2. 使用客户端B接收邮件

**预期结果**:
- 客户端B可以成功解密邮件
- 显示: "✓ 已加密并验证 [配对UA]"
- 邮件内容正确显示

### 测试6: 收件中心模式 - 不同UA识别

**目的**: 验证系统能识别并区分不同UA的邮件

**准备工作**:
- 客户端A配置:
  - 共享密钥: "SharedSecret2024"
  - UA身份: "team-alpha-001"
- 客户端C配置:
  - 共享密钥: "SharedSecret2024"
  - UA身份: "team-beta-002"（不同的身份）

**步骤**:
1. 使用客户端A发送加密邮件到收件中心账户
2. 使用客户端C尝试接收邮件

**预期结果**:
- 客户端C识别出这是其他UA的邮件
- 显示: "🔒 来自其他UA [无法解密]"
- 提示: "此邮件由其他UA发送，需要使用配对的UA才能查看"
- 邮件内容显示为无法解密的消息

### 测试7: 混合邮件接收

**目的**: 验证系统能正确处理三种类型的邮件

**准备工作**:
- 配置一个启用加密的客户端
- 准备接收以下三种邮件：
  1. 来自配对UA的加密邮件
  2. 来自其他UA的加密邮件
  3. 来自普通邮件客户端的未加密邮件

**步骤**:
1. 发送三种不同类型的邮件到同一收件箱
2. 使用客户端接收邮件

**预期结果**:
- 三种邮件都能正确识别和分类
- 配对UA邮件: "✓ 已加密并验证 [配对UA]"
- 其他UA邮件: "🔒 来自其他UA [无法解密]"
- 普通邮件: "📧 普通邮件（未加密）"

### 测试8: 多邮箱服务器兼容性

**目的**: 验证系统支持多种邮箱服务

**步骤**:
1. 配置QQ邮箱账号
2. 发送和接收邮件
3. 配置163邮箱账号
4. 发送和接收邮件
5. （可选）测试其他邮箱服务

**预期结果**:
- 所有邮箱服务都能正常工作
- 发送和接收功能正常
- 加密功能正常

### 测试9: 错误处理

**目的**: 验证系统的错误处理能力

**测试场景**:

1. **错误的密钥**:
   - 发送方使用密钥A
   - 接收方使用密钥B
   - 预期: 显示验证失败

2. **篡改检测**:
   - 手动修改加密邮件的内容（如果可能）
   - 预期: HMAC验证失败

3. **重复邮件**:
   - 多次接收同一封加密邮件
   - 预期: 第二次接收时显示重复警告

### 测试10: UI功能测试

**目的**: 验证图形界面的所有功能

**功能清单**:
- [x] 账号添加/删除/切换
- [x] 附件添加/删除
- [x] 高级设置保存
- [x] UA身份自动生成
- [x] 邮件列表显示
- [x] 邮件详情显示
- [x] 附件信息显示
- [x] 状态栏更新
- [x] 错误消息弹窗

## 性能测试

### 大附件测试

**步骤**:
1. 添加一个较大的文件作为附件（例如10MB）
2. 发送邮件
3. 接收邮件

**预期结果**:
- 能够成功处理大附件
- 加密和解密过程合理时间内完成

### 多附件测试

**步骤**:
1. 添加10个不同类型的附件
2. 发送邮件
3. 接收邮件

**预期结果**:
- 所有附件都能正确处理
- 附件列表正确显示

## 安全测试

### HMAC验证测试

**目的**: 验证消息认证功能

1. 尝试修改加密邮件内容
2. 系统应该检测到并拒绝

### 序列号防重放测试

**目的**: 验证防重放机制

1. 重复接收同一封邮件
2. 第二次应该显示重复警告

### UA身份验证测试

**目的**: 验证UA身份识别

1. 不同UA身份的邮件无法解密
2. 显示适当的提示信息

## 问题排查

### 常见问题

1. **无法发送邮件**:
   - 检查SMTP服务器和端口配置
   - 确认邮箱授权码正确
   - 检查网络连接

2. **无法接收邮件**:
   - 检查POP3服务器和端口配置
   - 确认邮箱中有邮件
   - 检查是否开启了POP3服务

3. **解密失败**:
   - 确认共享密钥相同
   - 确认UA身份相同（配对UA）
   - 检查是否是来自其他UA的邮件

4. **附件无法显示**:
   - 检查附件大小限制
   - 确认邮件接收完整

## 测试报告模板

```
测试日期: YYYY-MM-DD
测试人员: [姓名]
测试环境: [操作系统 + Python版本]

测试结果:
- 测试1 (基本邮件): [通过/失败] 
- 测试2 (附件功能): [通过/失败]
- 测试3 (安全加密): [通过/失败]
- 测试4 (附件加密): [通过/失败]
- 测试5 (配对UA): [通过/失败]
- 测试6 (不同UA): [通过/失败]
- 测试7 (混合邮件): [通过/失败]
- 测试8 (多服务器): [通过/失败]
- 测试9 (错误处理): [通过/失败]
- 测试10 (UI功能): [通过/失败]

发现的问题:
1. [问题描述]
2. [问题描述]

建议:
1. [改进建议]
2. [改进建议]
```

## 自动化测试脚本

由于需要实际的邮箱服务器，完整的自动化测试较难实现，但可以进行单元测试：

```python
# test_encoder.py
from email_encoder import create_encoder

def test_basic_encoding():
    encoder = create_encoder("test_secret", "ua_001")
    
    # 测试消息编码
    encoded = encoder.encode_secure_message("Hello World")
    assert encoded['secure'] == True
    assert encoded['sequence'] == 1
    
    # 测试消息解码
    decoded, verified, msg_type = encoder.decode_secure_message(encoded)
    assert decoded == "Hello World"
    assert verified == True
    assert msg_type == "paired"
    
def test_attachment_encoding():
    encoder = create_encoder("test_secret", "ua_001")
    
    # 测试附件编码
    test_data = b"Test file content"
    encoded = encoder.encode_attachment(test_data, "test.txt")
    assert encoded['secure'] == True
    
    # 测试附件解码
    decoded, verified, msg_type = encoder.decode_attachment(encoded)
    assert decoded == test_data
    assert verified == True

def test_ua_identity():
    encoder_a = create_encoder("test_secret", "ua_001")
    encoder_b = create_encoder("test_secret", "ua_002")
    
    # A编码
    encoded = encoder_a.encode_secure_message("Secret")
    
    # B尝试解码
    decoded, verified, msg_type = encoder_b.decode_secure_message(encoded)
    assert msg_type == "other_ua"
    assert verified == False

if __name__ == "__main__":
    test_basic_encoding()
    test_attachment_encoding()
    test_ua_identity()
    print("All tests passed!")
```

运行单元测试:
```bash
python test_encoder.py
```

## 总结

完成以上测试后，您应该能够验证系统的所有功能是否正常工作。如果发现任何问题，请参考问题排查部分或查看代码实现。
