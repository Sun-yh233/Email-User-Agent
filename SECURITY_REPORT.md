# 安全邮件实现报告

## 概述

本项目实现了一个具有安全发送与接收功能的邮件用户代理，支持MIME格式并实现了编码表映射关系的协商功能。系统采用一次一密(One-Time Pad variant)的方案，确保每封邮件使用不同的编码表，同时通过序列号机制保证在丢失、重复、乱序情况下的健壮性。

## 功能实现

### 1. MIME支持与编码表协商

#### 1.1 核心组件

- **EmailEncoder类**: 安全邮件编码器，实现基于共享密钥的一次一密编码
- **SecureMIMEBuilder类**: 安全MIME邮件构建器，负责构建和解析包含加密内容的MIME邮件

#### 1.2 密钥协商机制

系统基于以下安全假设：
- 通信双方A和B在线下通过安全渠道共享一个秘密密钥
- 该共享密钥被安全存储在各自的配置中
- 密钥长度建议至少16个字符

**密钥派生流程**：
1. 用户在GUI的"高级设置"中配置共享密钥
2. 系统使用HMAC-SHA256从共享密钥和消息序列号派生每封邮件的密钥
3. 派生公式：`KDF(K, seq, context) = HMAC-SHA256(K, seq || context)`
4. 每封邮件派生两个密钥：
   - `message_key = KDF(K, seq, "message")` - 用于生成编码表
   - `hmac_key = KDF(K, seq, "hmac")` - 用于消息认证

### 2. 一次一密的编码表机制

#### 2.1 编码表生成

每封邮件使用独立的Base64编码表：

```
编码表 = Generate_Table(message_key)
```

生成过程：
1. 将message_key的前8字节转换为整数作为随机数种子
2. 使用Python的Random类以该种子初始化随机数生成器
3. 对标准Base64字符表进行Fisher-Yates洗牌
4. 生成的64字符编码表确保每封邮件都不同

#### 2.2 序列号递增机制

- 发送方维护一个单调递增的序列号计数器
- 每发送一封邮件，序列号自动递增
- 序列号持久化存储在配置文件中，程序重启后继续递增
- 序列号范围：1 到 2^63-1（实际上可无限使用）

### 3. 健壮性保证

#### 3.1 防丢失

- 每封邮件包含独立的序列号
- 序列号不依赖于之前的邮件
- 即使中间某封邮件丢失，后续邮件仍可正常解密

#### 3.2 防重复

- 接收方维护一个已接收序列号集合
- 收到邮件时检查序列号是否已存在
- 重复邮件被拒绝，返回警告信息

#### 3.3 防乱序

- 每封邮件的序列号独立标识该邮件
- 不依赖邮件到达顺序
- 可以按任意顺序接收和解密邮件

#### 3.4 消息认证

使用HMAC-SHA256进行消息认证：

```
HMAC = HMAC-SHA256(hmac_key, sequence || encoded_content)
```

防护措施：
- 检测消息篡改
- 防止中间人攻击
- 使用constant-time比较防止时序攻击

### 4. 安全加密实现

#### 4.1 邮件结构

安全邮件采用标准MIME格式：

```
multipart/mixed (外层容器)
├── multipart/alternative (内容容器)
│   ├── text/plain (加密的纯文本，JSON格式)
│   │   Headers: X-Content-Secure: true
│   │   Content: {"sequence": N, "content": "...", "hmac": "...", "secure": true}
│   └── text/html (预留：加密的HTML)
└── application/octet-stream (预留：加密的附件)
```

#### 4.2 加密格式

每封安全邮件的JSON负载包含：
- `sequence`: 消息序列号
- `content`: 使用自定义Base64表编码的内容
- `hmac`: HMAC-SHA256消息认证码
- `version`: 协议版本（当前为"1.0"）
- `secure`: 安全标志（true）

#### 4.3 预留接口

**HTML内容加密**：
- `SecureMIMEBuilder.create_secure_email()` 已支持 `html_body` 参数
- HTML内容使用独立序列号加密
- 注意：当前实现中，一封邮件的文本和HTML部分会使用不同的序列号
- 建议改进：使用单个序列号加密整个邮件的所有部分

**附件加密**：
- `SecureMIMEBuilder.create_secure_email()` 已预留 `attachments` 参数
- 接口设计：每个附件使用独立序列号派生密钥
- 建议实现：
  ```python
  for attachment in attachments:
      att_key = encoder._derive_key(next_seq, f"attachment:{filename}")
      encrypted_data = encrypt_with_table(att_data, att_key)
  ```

## 安全性分析

### 使用的算法与协议

1. **HMAC-SHA256**
   - 来源：Python标准库 `hashlib` 和 `hmac`
   - 用途：密钥派生和消息认证
   - 安全性：SHA-256被NIST认证，HMAC构造提供了强认证保证

2. **Base64编码表映射**
   - 来源：自主实现，基于Python标准库 `base64`
   - 用途：内容混淆和编码
   - 安全性：虽不如AES等现代密码，但结合HMAC和一次一密提供了实用的安全性

3. **Fisher-Yates洗牌算法**
   - 来源：Python标准库 `random.shuffle`
   - 用途：生成随机编码表
   - 安全性：确保编码表的随机性和均匀分布

### 安全强度估计

#### 优势

1. **一次一密**: 每封邮件使用不同的编码表，即使一封邮件被破解也不影响其他邮件
2. **消息认证**: HMAC-SHA256提供强认证保证，防止篡改
3. **序列号机制**: 有效防止重放、重复和乱序攻击
4. **密钥派生**: HMAC-based KDF是业界认可的安全密钥派生方法

#### 局限性

1. **Base64替换密码**: 不如AES等现代分组密码安全，理论上存在频率分析攻击的可能性
2. **密钥管理**: 依赖用户手动配置共享密钥，需要安全的线下渠道
3. **前向安全性**: 如果共享密钥泄露，所有历史邮件可被解密
4. **序列号存储**: 接收方需要存储所有已接收序列号，长期运行可能导致内存增长（建议定期清理）
5. **HTML多序列号**: 当前实现中HTML和文本使用不同序列号，可能导致同步问题

#### 安全等级评估

- **对抗普通窃听**: 高安全性 ⭐⭐⭐⭐⭐
- **对抗主动攻击**: 高安全性 ⭐⭐⭐⭐⭐（HMAC认证）
- **对抗密码分析**: 中等安全性 ⭐⭐⭐⭐（Base64替换）
- **对抗量子计算**: 低安全性 ⭐⭐（SHA-256在量子计算下安全性降低）

### 改进建议

未来可以考虑的增强：

1. **升级加密算法**: 使用AES-GCM替代Base64表映射
2. **完善密钥管理**: 实现Diffie-Hellman密钥交换
3. **前向安全**: 实现密钥轮换机制
4. **后量子密码**: 引入后量子安全的密钥交换算法
5. **统一序列号**: 改进HTML加密使用单个序列号
6. **序列号管理**: 实现滑动窗口或周期性清理机制
7. **性能优化**: 使用字典缓存字符表反向映射

## 使用说明

### 发送方配置

1. 打开"设置" -> "高级设置"
2. 勾选"启用自定义Base64编码"
3. 输入共享密钥（与接收方约定）
4. 保存设置

### 接收方配置

1. 同样在"高级设置"中启用并输入相同的共享密钥
2. 系统自动处理序列号和密钥派生

### 发送安全邮件

1. 配置完成后，正常撰写邮件
2. 点击"发送"，系统自动加密
3. 邮件在外观上看起来是普通MIME邮件，但内容已加密

### 接收安全邮件

1. 点击"接收邮件"
2. 系统自动识别安全邮件（X-Secure-Email头）
3. 自动解密并验证
4. 显示"安全状态"信息：
   - ✓ 已加密并验证：邮件完整且未被篡改
   - ⚠️ 加密但验证失败：邮件可能被篡改或密钥错误

## 技术细节

### 编码流程

```
原始文本 → UTF-8编码 → 标准Base64 → 字符表映射 → 编码文本
```

### 解码流程

```
编码文本 → 字符表反向映射 → 标准Base64 → UTF-8解码 → 原始文本
```

### 完整发送流程

```
1. 用户输入邮件内容
2. 序列号递增：seq = sent_sequence + 1
3. 密钥派生：
   - message_key = HMAC-SHA256(shared_secret, "seq:message")
   - hmac_key = HMAC-SHA256(shared_secret, "seq:hmac")
4. 生成编码表：table = GenerateTable(message_key)
5. 编码内容：encoded = EncodeWithTable(content, table)
6. 计算HMAC：mac = HMAC-SHA256(hmac_key, "seq:encoded")
7. 打包JSON：{"sequence": seq, "content": encoded, "hmac": mac, "secure": true}
8. 构建MIME邮件
9. 通过SMTP发送
10. 保存序列号到配置
```

### 完整接收流程

```
1. 通过POP3接收邮件
2. 解析MIME结构
3. 检查X-Secure-Email头
4. 提取JSON负载
5. 检查序列号是否重复
6. 密钥派生（使用相同的共享密钥和序列号）
7. 验证HMAC
8. 生成解码表
9. 解码内容
10. 记录序列号
11. 显示给用户
```

## 兼容性说明

### 与普通邮件客户端的兼容性

- 安全邮件使用标准MIME格式
- 其他邮件客户端可以接收和显示（显示为JSON文本）
- 不会影响邮件的正常传输

### 与本客户端未启用安全模式的兼容性

- 可以同时发送普通邮件和安全邮件
- 未配置共享密钥时，自动使用普通模式
- 接收时自动识别邮件类型

## 测试验证

系统已通过以下测试：

1. ✅ 基本编码/解码测试
2. ✅ 一次一密（序列号递增）测试
3. ✅ 防重放攻击测试
4. ✅ HMAC验证测试
5. ✅ 错误密钥检测测试
6. ✅ MIME构建和解析测试
7. ✅ 中文字符支持测试
8. ✅ 多邮件序列测试

## 结论

本实现成功满足了所有需求：

1. ✅ 发送与接收均支持MIME
2. ✅ 实现了编码表映射关系的协商（基于共享密钥）
3. ✅ 每封邮件使用不同的编码表（一次一密）
4. ✅ 支持丢失、重复、乱序的健壮性
5. ✅ 对整体内容进行加密
6. ✅ 预留了附件和HTML加密接口

系统提供了实用的安全性，适合对通信保密性有一定要求但不需要军事级加密的场景。在正确配置和使用的情况下，可以有效保护邮件内容免受窃听和篡改。
